const usersCtrls = require("./users");
const newsCtrls = require("./news");
const petsCtrls = require("./pets");
const noticesCtrls = require("./notices");

module.exports = {
  usersCtrls,
  newsCtrls,
  petsCtrls,
  noticesCtrls,
};
